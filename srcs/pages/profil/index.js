var menuId = 0;

var initMenuProfil = function() {
  let menus = document.querySelector("#profil-menu-wrapper").childNodes;

  NodeList.prototype.forEach = Array.prototype.forEach;

  menus.forEach(function(child) {
  })
}
